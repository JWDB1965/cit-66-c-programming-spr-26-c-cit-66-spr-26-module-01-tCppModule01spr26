
#include <iostream>
using namespace std;

int main() {
    int first, second;
    cout << "Enter two integers separated by a space: ";
    cin >> first >> second;
    cout << "The sum is: " << (first + second) << endl;
    return 0;
}
